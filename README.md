# Сайт-резюме в стиле мессенджера

Современный сайт-резюме с интерфейсом в стиле Discord мессенджера, созданный на React с TypeScript и анимациями.

## 🚀 Особенности

- **Интерфейс мессенджера**: Боковая панель с разделами и окно чата с сообщениями
- **Темная тема Discord**: Аутентичный дизайн с цветовой схемой Discord
- **Плавные анимации**: Анимации появления сообщений, индикатор печати, переходы
- **Адаптивный дизайн**: Полностью адаптирован для мобильных устройств
- **TypeScript**: Полная типобезопасность
- **Оптимизация**: Быстрая сборка с Vite

## 📋 Требования

- Node.js 18+ и npm
- Ubuntu сервер с Nginx (для развертывания)

## 🛠️ Установка

1. **Клонируйте репозиторий или перейдите в папку проекта:**
   ```bash
   cd WebApp
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте данные резюме:**
   
   Откройте файл `src/data/resume.ts` и заполните своими данными:
   - Имя и должность
   - О себе
   - Навыки (с уровнем от 1 до 5)
   - Опыт работы
   - Образование
   - Контакты

4. **Запустите проект в режиме разработки:**
   ```bash
   npm run dev
   ```

   Откройте браузер по адресу `http://localhost:5173`

5. **Соберите проект для продакшена:**
   ```bash
   npm run build
   ```

   Собранные файлы будут в папке `dist/`

## 📦 Развертывание на Ubuntu сервере

### Шаг 1: Подготовка сервера

1. **Установите Nginx (если еще не установлен):**
   ```bash
   sudo apt update
   sudo apt install nginx -y
   ```

2. **Создайте директорию для сайта:**
   ```bash
   sudo mkdir -p /var/www/resume-messenger
   ```

3. **Скопируйте собранные файлы на сервер:**
   
   С вашего локального компьютера:
   ```bash
   scp -r dist/* user@your-server-ip:/var/www/resume-messenger/
   ```
   
   Или используйте любой другой способ передачи файлов (FTP, rsync и т.д.)

4. **Установите права доступа:**
   ```bash
   sudo chown -R www-data:www-data /var/www/resume-messenger
   sudo chmod -R 755 /var/www/resume-messenger
   ```

### Шаг 2: Настройка Nginx

1. **Скопируйте конфигурацию Nginx:**
   ```bash
   sudo cp nginx.conf /etc/nginx/sites-available/resume-messenger
   ```

2. **Отредактируйте конфигурацию:**
   ```bash
   sudo nano /etc/nginx/sites-available/resume-messenger
   ```
   
   Измените:
   - `server_name` на ваш домен или IP адрес
   - При необходимости измените `root` путь

3. **Создайте символическую ссылку:**
   ```bash
   sudo ln -s /etc/nginx/sites-available/resume-messenger /etc/nginx/sites-enabled/
   ```

4. **Проверьте конфигурацию Nginx:**
   ```bash
   sudo nginx -t
   ```

5. **Перезапустите Nginx:**
   ```bash
   sudo systemctl restart nginx
   ```

6. **Проверьте статус Nginx:**
   ```bash
   sudo systemctl status nginx
   ```

### Шаг 3: Настройка SSL (опционально, но рекомендуется)

Для использования HTTPS с Let's Encrypt:

```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

Certbot автоматически обновит конфигурацию Nginx для использования HTTPS.

## 🔧 Настройка данных резюме

Все данные резюме находятся в файле `src/data/resume.ts`. Структура данных:

```typescript
export const resumeData: ResumeData = {
  name: "Ваше Имя",
  title: "Ваша Должность",
  about: "Текст о себе...",
  skills: [
    { name: "JavaScript", level: 5 },
    // ...
  ],
  experience: [
    {
      company: "Компания",
      position: "Должность",
      period: "2020 - 2022",
      description: ["Описание 1", "Описание 2"]
    }
  ],
  education: [
    {
      institution: "Университет",
      degree: "Степень",
      period: "2016 - 2020",
      description: "Описание"
    }
  ],
  contacts: [
    { type: "Email", value: "email@example.com", link: "mailto:email@example.com" }
  ]
};
```

### Добавление аватара

Вы можете добавить аватар, указав путь к изображению:

```typescript
avatar: "/path/to/avatar.jpg"
```

Поместите изображение в папку `public/` и укажите путь относительно корня (например, `/avatar.jpg`).

## 📱 Адаптивность

Сайт полностью адаптирован для мобильных устройств:
- Выдвижная боковая панель на мобильных
- Оптимизированные размеры шрифтов
- Адаптивная сетка компонентов

## 🎨 Кастомизация темы

Цвета и стили можно изменить в файле `src/styles/theme.css`. Все цвета определены как CSS переменные:

```css
:root {
  --discord-bg-primary: #36393f;
  --discord-accent: #5865f2;
  /* и т.д. */
}
```

## 📝 Скрипты

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm run preview` - Предпросмотр собранного проекта

## 🐛 Решение проблем

### Проблема: Белая страница после развертывания

**Решение:** Убедитесь, что в конфигурации Nginx есть строка `try_files $uri $uri/ /index.html;` для обработки React Router.

### Проблема: 403 Forbidden

**Решение:** Проверьте права доступа к файлам:
```bash
sudo chown -R www-data:www-data /var/www/resume-messenger
sudo chmod -R 755 /var/www/resume-messenger
```

### Проблема: Статические файлы не загружаются

**Решение:** Проверьте, что все файлы из папки `dist/` скопированы на сервер, включая папку `assets/`.

## 📄 Лицензия

Этот проект создан для личного использования.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, проверьте:
1. Правильность настройки данных в `resume.ts`
2. Корректность конфигурации Nginx
3. Права доступа к файлам на сервере
4. Логи Nginx: `/var/log/nginx/resume-messenger-error.log`
